# {{brand.name}} — reel strategy

<!-- Filled from research.md. The user picks rows from this table; only picked rows get plates and comps. Keep to 5–8 proposals; one hook each. Columns are fixed — tooling parses them. -->

Budget: {{n}} reels · {{durationSeconds}}s each · 1080×1920 · silent

| # | hook | audience | objection it answers | beats | grammar + opener | plates | CTA |
|---|---|---|---|---|---|---|---|
| 1 | "{{line that lands in 0–3s}}" | {{who / placement}} | {{the doubt in their words}} | {{beat1}} → {{beat2}} → {{beat3}} | {{whip/through/deck/jump/rise}} + {{snap/settle/bloom/typeLine}} | {{plate names, e.g. send, map}} | {{verb → domain/path}} |
| 2 | … | … | … | … | … | … | … |

Column notes:
- **hook** — on-screen words, not a summary. Must be complete by 3s.
- **audience** — one segment + placement (organic / cold paid / retarget after #n).
- **objection it answers** — one; if two, split the row.
- **beats** — 3–5 short on-screen lines after the hook, in order.
- **grammar + opener** — camera grammar for the plate (`whip`, `through`, `deck`, `jump`, `rise`) and the headline opener (`snap`, `settle`, `bloom`, `typeLine`).
- **plates** — which real-app recordings the reel needs; new names mean a new plate definition.
- **CTA** — verb + destination, ≤6 words.

## Claims check

Every hook and beat above checked against profile `neverClaim` and the tier table: {{pass / list of edits made}}.

## Recommendation

Ship order and why: {{…}}. Cuts of one message (a/b/c): {{…}}.
